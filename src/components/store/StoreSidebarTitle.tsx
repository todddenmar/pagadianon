import React from 'react';

function StoreSidebarTitle({ text }: { text: string }) {
  return <div className="text-base font-semibold px-4 py-2">{text}</div>;
}

export default StoreSidebarTitle;
