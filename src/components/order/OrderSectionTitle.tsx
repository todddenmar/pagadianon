import React from 'react';

function OrderSectionTitle({ text }: { text: string }) {
  return <div className="text-lg md:text-xl font-semibold">{text}</div>;
}

export default OrderSectionTitle;
